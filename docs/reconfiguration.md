# Recovery Notes

Detailed recovery and reconfiguration notes are intentionally omitted from the
public `main` branch. The implementation under `sim/control/` remains the
reference for the current experimental behavior.
