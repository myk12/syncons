[project]
name = "syncons"
version = "0.1.0"
description = "Experimental distributed-protocol simulator and FPGA prototype."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "seaborn>=0.13.2",
]
